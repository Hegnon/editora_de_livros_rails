

 <%= form_tag(livros_path, method: :get) do %>
   <p> Filtrar por:
     <%= radio_button_tag 'search_type', 'livro_titulo', true %>
     Titulo
     <%= radio_button_tag 'search_type', 'autor_nome' %>
     Autor (nome)
     <%= text_field_tag 'search', nil, placeholder: 'Digite o título ou autor do livro...' %>
     <%= submit_tag "Filtrar" %>
   </p>
 <% end %>

<h1>Livros</h1>

<div id="livros">
  <% @livros.each do |livro| %>
    <%= render livro %>
    <p>
      <%= link_to "Show this livro", livro %>
    </p>
  <% end %>
</div>

<%= link_to "New livro", new_livro_path %>
